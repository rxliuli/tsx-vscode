{
  "name": "tsx",
  "displayName": "tsx",
  "description": "esbuild-kit tsx integrated",
  "version": "0.1.2",
  "publisher": "rxliuli",
  "engines": {
    "vscode": "^1.71.0"
  },
  "categories": [
    "Other"
  ],
  "icon": "public/logo.png",
  "scripts": {
    "vscode:prepublish": "pnpm build",
    "build": "esbuild ./src/extension.ts --outfile=./dist/extension.js --bundle --sourcemap --external:vscode --platform=node --format=cjs",
    "dev": "pnpm build --watch",
    "pretest": "pnpm build",
    "release": "pnpm build && vsce package --no-dependencies"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/rxliuli/tsx"
  },
  "main": "./dist/extension.js",
  "activationEvents": [
    "onCommand:tsx.runOnSave",
    "onCommand:tsx.runOnce"
  ],
  "contributes": {
    "commands": [
      {
        "command": "tsx.runOnSave",
        "title": "Run on save for Current File",
        "category": "tsx",
        "icon": "$(debug-start)"
      },
      {
        "command": "tsx.stopCurrent",
        "title": "Stop current",
        "category": "tsx",
        "icon": "$(debug-stop)"
      }
    ],
    "menus": {
      "editor/title/run": [
        {
          "command": "tsx.runOnSave",
          "when": "editorLangId =~ /(typescript|typescriptreact|javascript|javascriptreact)/ && !tsx.runner"
        },
        {
          "command": "tsx.stopCurrent",
          "when": "editorLangId =~ /(typescript|typescriptreact|javascript|javascriptreact)/ && tsx.runner"
        }
      ]
    }
  },
  "devDependencies": {
    "@types/node": "16.x",
    "@types/vscode": "^1.71.0",
    "@types/which": "^2.0.1",
    "@vscode/test-electron": "^2.1.5",
    "esbuild": "^0.15.7",
    "prettier": "^2.7.1",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "tsx": "^3.9.0",
    "which": "^2.0.2"
  }
}
